"use strict"

const repository = {
  create: () => {
    new Promise((resolve, reject) => {
      resolve(res);
      }).then((res) => {
        done();
      }).catch((err) => {
        done(err);
      });
      return new Promise;
  }
 };

module.exports = repository;
